using OpenTK.Graphics.OpenGL4;
using System;
using System.Collections.Generic;
using System.Text;

namespace GameDesignWorkshop.Scripts.Engine.Listeners
{
    public class MouseListener
    {
        /*private double scrollX, scrollY;
        private double yPos, xPos, lastY, lastX;
        private bool[] mouseButtonPressed = new bool[3];
        private bool isDragging;

        private MouseListener()
        {
            this.scrollX = 0.0;
            this.scrollY = 0.0;
            this.lastX = 0.0;
            this.lastY = 0.0;
            this.xPos = 0.0;
            this.yPos = 0.0;
        }

        public void mousePosCallback(long window, double xpos, double ypos)
        {
            this.lastX = this.xPos;
            this.lastY = this.yPos;

            this.xPos = xpos;
            this.yPos = ypos;
        }

        public void mouseButtonCallback(long window, int button, int action, int mods)
        {
            if (action == GL.Pr)
            this.mouseButtonPressed[button] = true;

        }*/
    }
}
